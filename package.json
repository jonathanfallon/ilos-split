{
  "name": "ilos-split",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "start": "yarn workspace @split/kernel ilos http 8080",
    "build": "lerna run --sort build",
    "build:split": "lerna run --scope @split/* --sort build"
  },
  "license": "Apache-2.0",
  "workspaces": [
    "split/kernel",
    "split/services/*",
    "ilos/packages/*"
  ]
}
