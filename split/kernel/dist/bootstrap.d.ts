/**
 * TODO
 *
 * import the kernel
 * import the transports
 * export a created bootstrap
 */
import { Bootstrap as BaseBootstrap } from "@ilos/framework";
export declare const bootstrap: BaseBootstrap;
